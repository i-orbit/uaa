spring:
  application:
    name: uaa
  profiles:
    active: dev
  cloud:
    nacos:
      server-addr: 192.168.26.130:8848
      username: nacos
      password: nacos
      discovery:
        namespace: ${spring.profiles.active}
      config:
        server-addr: 192.168.26.130:8848
        namespace: ${spring.profiles.active}
        file-extension: yaml
        extension-configs:
          - common.yaml
          - redis.yaml
          - rabbitmq.yaml
          - influxdb.yaml